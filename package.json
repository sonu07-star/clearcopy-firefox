{
  "name": "clearcopy-firefox",
  "version": "1.1.0",
  "private": true,
  "description": "Development and release checks for ClearCopy.",
  "scripts": {
    "test": "node tests/cleaner.test.js && node tests/background.test.js",
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest --ignore-files package.json package-lock.json tests listing-assets README.md PRIVACY.md LICENSE CHANGELOG.md RULES.md CONTRIBUTING.md SECURITY.md RELEASE_NOTES_*.md",
    "check": "npm test && npm run lint"
  },
  "devDependencies": {
    "web-ext": "10.4.0"
  }
}
